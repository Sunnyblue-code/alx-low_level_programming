#include "main.h"
OBOBOB
/**
OBOBOB * _abs - compute absolute value of integer
 * @n: integer argument
 * Return: absolute value
 */
OAOAOA
OAOAOAint _abs(int n)
{
	if (n > 0)
		return (n);
	else if (n < 0)
		return (-n);

	return (0);
}
