#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - entry point
 * Description: prints numbers from 1 to 10
 * Return: 0
*/
int main(void)
{
int base;
    for (base = 0; base < 10; base++)
	{
        printf("%d", base);
	}
    	putchar('\n');
    return (0);
}
